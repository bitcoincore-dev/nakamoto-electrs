[advisories]
# RUSTSEC-2024-0437: protobuf 2.x crash via uncontrolled recursion.
# This is a transitive dependency: electrs -> prometheus 0.13.x -> protobuf 2.x.
# prometheus 0.13.x requires protobuf 2.x and there is no compatible upgrade path
# until electrs or prometheus upgrades to protobuf 3.x.
ignore = ["RUSTSEC-2024-0437"]
